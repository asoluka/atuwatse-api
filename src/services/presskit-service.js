const { presskit } = require("../models/presskit-model");

const getPresskit = () => {
  return presskit;
};

const addPresskit = (presskit) => {
  return presskit;
};

module.exports = {
  getPresskit,
  addPresskit,
};
